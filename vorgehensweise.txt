DOCX zu HTML konvertiert (Word: Save as...)
Python aufgesetzt:
	venv mit BeautifulSoup (parser library) und lxml im Hintergrund (schneller XML Parser) für später
Text aufgeteilt auf ein HTML pro Publikation (ein h1 ist eine Publikation)
Encodings getestet - <meta http-equiv=Content-Type content="text/html; charset=windows-1252"> im Header ist im Browser lesbar. UTF-8 Konvertierung ist also nicht nötig.

Im Folgenden ist eine ungefähre Zuordnung gegeben.
Die HTML Elemente entsprechen dem minimalen Pfad, der für dieses Feld benötigt wird.
Alle Kinder davom müssen dann als String zurückgegeben werden, wobei alle HTML Tags entfernt wurden (BeautifulSoupObject.get_text()).

Titel des Journals = h1.a.span 
	{Kürzel (x Buchstaben)} {Bindestrich} {Titel} ({Ort})

Ausgabe = h2.span
